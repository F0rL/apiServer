const {apiDomain, staticDomain} = require('./domain')

const hpApiDomain = `${apiDomain}/homepage`
const hpStaticDomain = `${staticDomain}/homepage`

module.exports = {
  hpApiDomain,
  hpStaticDomain
}